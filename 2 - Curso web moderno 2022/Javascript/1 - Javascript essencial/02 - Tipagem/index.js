/*

O javascript é dinamicamente tipado, o que significa que nós não precisamos declarar seu tipo. Mas isso só é possível porque ele mesmo identifica o tipo da variável que estamos declarando e o atribui, mesmo que não explicitamente. 

Por exemplo:

const cliente = "Juliana"


*/


let qualquer = 'legal'
console.log(qualquer)
console.log(typeof(qualquer))

// para saber o tipo da varuavel usamos (typeof(nome da variavel))


const age = 22

console.log(age)
console.log(typeof(age))

/* 

Evitar nomes genericos e siglas para as variaveis


*/