/*

Var => A variavel (var) é uma variavel global que pode ser altterada a todo momento.

let => A variavel (let) é uma variavel que pode ter o seu valor alterado.

const => A constante (const) é uma variavel/constante que nao pode ter seu valor alterado, uma vez que seu valor foi definido ele tera aquele valor ate o fim do projeto/codigo.

*/


let name = "Matheus"
var state = "São paulo"
const age = 22

console.log(name,state,age + " anos")